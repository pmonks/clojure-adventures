(ns org.pmonks.chain-reaction.core-test
  (:require [clojure.test :refer :all]
            [org.pmonks.chain-reaction.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
